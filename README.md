# Backend
cd server
npm install

# Frontend (en otra terminal)
cd client
npm install

# Correr server
cd server
node index.js

# Correr cliente
cd client
npm run dev  # O 'npm start' si usas Create React App
